# siping

Clean Python port target for the legacy SIPing system.

- Legacy reference code lives in `../siping/legacy/`
- This package is the new implementation.

## Dev

```bash
cd siping
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
siping
```
