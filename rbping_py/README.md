# rbping_py

Clean Python port target for the legacy RBPing system.

- Legacy reference code lives in `../rbping/legacy/`
- This package is the new implementation.

## Dev

```bash
cd rbping_py
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
rbping
```
